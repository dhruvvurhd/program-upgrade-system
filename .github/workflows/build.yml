name: Build Solana Program

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04
    
    steps:
    - uses: actions/checkout@v4
      
    - uses: dtolnay/rust-toolchain@stable
      with:
        toolchain: 1.82.0
      
    - name: Install Solana
      run: |
        wget https://github.com/solana-labs/solana/releases/download/v1.18.17/solana-release-x86_64-unknown-linux-gnu.tar.bz2
        tar jxf solana-release-x86_64-unknown-linux-gnu.tar.bz2
        echo "$PWD/solana-release/bin" >> $GITHUB_PATH
        
    - name: Install Anchor CLI via AVM
      run: |
        cargo install --git https://github.com/coral-xyz/anchor avm --force
        avm install 0.30.1
        avm use 0.30.1

    - name: Update Anchor.toml for CI
      run: |
        sed -i 's/anchor_version = "0.32.1"/anchor_version = "0.30.1"/' Anchor.toml

    - name: Build Program
      run: |
        export PATH="$HOME/.avm/bin:$PATH"
        anchor build
        
    - name: Copy artifacts
      run: |
        mkdir -p artifacts
        cp target/deploy/*.so artifacts/ 2>/dev/null || echo "No .so files found"
        
    - uses: actions/upload-artifact@v4
      with:
        name: program-build
        path: |
          artifacts/*.so
          target/idl/*.json
